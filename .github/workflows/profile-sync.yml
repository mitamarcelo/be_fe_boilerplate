name: Sync Profile Stats

on:
  push:
    branches:
      - main
      - master

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: mitamarcelo/profile-sync-action@v1
        with:
          token: ${{ secrets.PROFILE_SYNC_TOKEN }}